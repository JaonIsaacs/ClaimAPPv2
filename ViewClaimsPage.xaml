<Page x:Class="ClaimAPPv2.ViewClaimsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ClaimAPPv2"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ViewClaimsPage">

    <Grid>
        <ListView x:Name="ClaimsListView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}" />
                    <GridViewColumn Header="Document" DisplayMemberBinding="{Binding DocumentPath}" />
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" />
                    <!-- Display  -->
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
